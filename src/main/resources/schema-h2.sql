CREATE TABLE mashData (measurementTime timestamp NOT NULL,mash float ,flow float ,herms float );
CREATE TABLE runStatus (running boolean);
CREATE TABLE targetTemps (secondsElapsed int(11),  temperature float);
